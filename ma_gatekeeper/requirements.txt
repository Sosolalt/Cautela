# Core agent + ADK
google-adk>=1.0.0
google-genai>=0.4.0
google-cloud-aiplatform>=1.50.0
google-auth>=2.20.0  # for OIDC verification on /reflect

# Arize Phoenix observability + evals
arize-phoenix>=4.0.0
arize-phoenix-client>=1.17.0  # client.spans.add_span_annotation (not deprecated annotations.*)
arize-phoenix-evals>=0.5.0
arize-phoenix-otel>=0.5.0
openinference-instrumentation-google-adk>=0.1.11
openinference-instrumentation-google-genai>=0.1.0
openinference-instrumentation-vertexai>=0.1.0
openinference-instrumentation-mcp>=0.1.0

# OpenTelemetry
opentelemetry-sdk>=1.20.0
opentelemetry-exporter-otlp>=1.20.0
opentelemetry-proto>=1.20.0

# MCP (for the Reflector's @arizeai/phoenix-mcp tool wiring)
mcp>=0.9.0

# SEC EDGAR
edgartools[ai]>=4.0.0

# Data + math
pandas>=2.0.0
numpy>=1.25.0
# Used by scripts/perturb_contracts.py (offline leakage discriminator) +
# scripts/calibrate.py (one-sided Wilson + cluster bootstrap helpers).
scikit-learn>=1.3.0
pydantic>=2.5.0
datasets>=2.16,<4.0  # datasets>=4 removed script-dataset support; CUAD/MAUD are script datasets needing trust_remote_code=True (only on <4)
huggingface-hub>=0.20.0

# PDF parsing (fallback path). The agent feeds artifacts to Gemini via
# Part.from_bytes (inline) under 8 MB and via the Files API + Part.from_uri
# above that threshold — see agent/server.py:_build_gemini_part.
pdfplumber>=0.10.0

# Web server
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.9

# Plotting reliability diagrams
matplotlib>=3.8.0
